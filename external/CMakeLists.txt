###################################################################
# /external	                                                      #
###################################################################

include( glfw.cmake )

include( glad.cmake )

include( zlib.cmake )
include( libpng.cmake )

include( nfd.cmake )

include( gtest.cmake )

#include(freetype.cmake)